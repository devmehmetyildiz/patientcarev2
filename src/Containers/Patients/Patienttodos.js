import React from 'react'
import { connect } from 'react-redux'
import Patienttodos from '../../Pages/Patients/Patienttodos'



const mapStateToProps = (state) => ({

})

const mapDispatchToProps = {

}

export default connect(mapStateToProps, mapDispatchToProps)(Patienttodos)